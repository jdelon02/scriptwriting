# Only the primary store is uploaded to. Other IDs are reserved for later use.
primary_vector_store_id=
vector_stores='[]'

# Base URL of litellm-pgvector (not the embedding proxy).
rag_base_url=
# Leave blank to reuse Hermes model.api_key; set if the vector service differs.
rag_api_key=
